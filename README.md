# clandarkside.com

A Hugo powered website showing our history, including matches, and various other content related to Team Darkside.

CSS cache note: if style changes do not show up in some browsers, bump `params.cssVersion` in `config.toml` to force a fresh `site.css` fetch.
